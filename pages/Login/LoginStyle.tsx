import { StyleSheet } from "react-native";
const loginStyle = StyleSheet.create({
  loginForm: {
    marginTop: 10,
  },
  inputField: {
    marginTop: 20,
    // borderColor:
  },
});
export default loginStyle;
